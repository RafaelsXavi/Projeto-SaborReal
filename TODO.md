# Render Deploy TODO for Projeto-SaborReal\n\n✅ JWT_SECRET generated: wf5GtTPC4emH/w77QPZOPY0X6OPVDtj0sQPJM144e2fBC/+tOO7cSzNMNjDy2okKvQqvIms/9PQhZ196kQ3XIg==\n\n## 1. Prep (Local) [2/4]\n✅ [x] Secrets ready\n- [ ] `npm install`\n- [ ] Fix Docker: Restart Docker Desktop → `docker compose ps`\n- [ ] `npm run docker:up`\n- [ ] Migrate/Seed: `docker compose exec api npm run db:migrate:prod`, `docker compose exec api npm run seed:catalog`\n\n## 2. GitHub\n- [ ] `git init`, `git add .`, `git commit -m \"Deploy ready\"`\n- [ ] Create GitHub repo (public), `git remote add origin https://github.com/YOUR_USERNAME/projeto-saborreal.git`, `git push -u origin main`\n\n## 3. Render [0/4]\n- [ ] Edit render.yaml: Replace YOUR_USERNAME → your GitHub\n- [ ] render.com account → New → Blueprint → Connect GitHub → Select repo\n- [ ] Set envs: DATABASE_URL (from Postgres service), JWT_SECRET (above), CORS_ORIGINS=https://saborreal-web.onrender.com (update post-deploy)\n- [ ] Deploy → Logs → Run migrate/seed in shell if needed\n\n## 4. Post-Deploy Verify\n- [ ] API: https://saborreal-api.onrender.com/healthz → 200\n- [ ] Catalog: /v1/catalog → items/categories\n- [ ] Web: https://saborreal-web.onrender.com → Load menu, register/order flow\n\n**Commands to run:** `npm run docker:ps` | `git status`
